{
  "installCommand": "rm -rf node_modules && npm install",
  "buildCommand": "npx tsc --noEmit false && npx vite build",
  "outputDirectory": "dist",
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] && exit 0 || exit 1",
  "framework": "vite"
}